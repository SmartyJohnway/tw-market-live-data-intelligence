# V1 release-readiness root-artifact archive

These files were retained for historical provenance but moved out of the
repository root during V1 release-readiness preparation.

- `mode_a_fixes.patch` is a historical patch snapshot, not an active apply
  instruction or release input.
- `FINAL_DELIVERY_REPORT.md` describes an earlier delivery state and is not a
  current product or release authority.

Current product and release entry points are the root `README.md`,
`docs/INDEX.md`, and `docs/release/`.
