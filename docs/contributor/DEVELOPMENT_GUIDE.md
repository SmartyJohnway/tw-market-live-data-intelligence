# Development Guide

M5R is documentation/release hardening. Product behavior, source semantics, contracts, FastAPI endpoint contracts, MCP tool contracts, frontend behavior, source adapters, source-health logic, conversation builder logic, and M5F content are out of scope unless a future approved task explicitly changes them.

Before implementing, search for existing contracts and tests with `rg`. Prefer extending product docs over duplicating stale milestone reports.
