# M8R-03E-R1 M8 Post-M8C Repository Realignment and Health Audit Protocol

Baseline SHA: `bd3496efe7492e6cd3c7dacc169e142f90e6cd92`.

This protocol records the R1 gate that realigns the repository from a governed AI conversation system toward a governed market evidence platform plus portable AI capability skill and agent-selectable tools.

## Execution baseline

Commands required before modification were executed after configuring the missing `origin` remote: `git fetch origin`, `git checkout main`, `git pull --ff-only origin main`, `git status --short`, `git rev-parse HEAD`, and `git rev-parse origin/main`. Both HEAD and origin/main resolved to `bd3496efe7492e6cd3c7dacc169e142f90e6cd92`.

## Boundary

R1 is inventory, verification, low-risk metadata cleanup, risk classification, and remediation planning. It does not implement Phase B, MCP, new market data sources, scheduler/polling, persistent watchlists, broad runtime rewrites, or schema-breaking removals.

## Acceptance disposition

Final R1 disposition is GO_WITH_CAVEATS, subject to the machine-readable health status and technical-debt register.
