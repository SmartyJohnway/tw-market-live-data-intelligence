node_modules/
.env
__pycache__/
*.pyc
.venv/
venv/
env/

# Runtime execution artifacts
# These are generated by the watchlist executor and must not be committed.
artifacts/m8r_03d/
artifacts/m8r_03d_authorization_consumption/
artifacts/debug_exec*/
artifacts/e2e_test/
artifacts/plugin_activation_test/
artifacts/zero_code_test/
artifacts/latest_operator_acceptance_report.json
artifacts/latest_operator_acceptance_report.md

# Authorization consumption receipts are single-use and must not be versioned.
# They are managed by the runtime retention policy (30-day default).
artifacts/**/*.json

# Research live observation runs (non-canonical, not fixtures)
research/live_observation_runs/latest_*/
