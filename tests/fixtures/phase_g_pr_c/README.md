# Phase G PR-C Test Fixtures

These files are a **NON-AUTHORITATIVE TEST SNAPSHOT**. They prove historical
Mode C V1 Result, Audit, and Markdown bytes remain readable and unchanged while
the repository gains an explicit V2-capable projection path.

Runtime code never loads this directory as contract authority. Production
schemas under `schemas/` remain authoritative.
